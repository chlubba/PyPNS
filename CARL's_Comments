- CAP only gets recorded if h5py is installed. Do in terminal:
	sudo apt-get install libhdf5-dev
	pip install h5py

... if you don't have pip:
    go to https://pip.pypa.io/en/stable/installing/
    download pip
    type in terminal:
        python get-pip.py
- nrnivmodl needs to be run with all modules at once, not one after the other.
    nrnivmodl AXNODE.mod xtra.mod vecevent.mod
- run bundle_general_3.py with flags and properties set as needed
